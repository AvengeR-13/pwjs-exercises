function getTotalNumberOfNewYaersEveSundays(a, b) {
    const y = b-a;
    let r = 0;
    for (let i = 0; i <= y; i++) {
        const x = new Date(a, 0, 1);
        if (x.getDay() == 0) {
            r += 1;
        }
        a++;
    }
    return r;
}
